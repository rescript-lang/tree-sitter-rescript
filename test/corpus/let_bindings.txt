============================================
Plain
============================================

let a = 5
let b = a
let c = #foo

---

(source_file
  (let_binding (identifier) (number))
  (let_binding (identifier) (identifier))
  (let_binding (identifier) (polyvar)))

============================================
Tuple destructuring
============================================

let (a, b, (c, d)) = foo

---

(source_file
  (let_binding
    (tuple_pattern
      (identifier)
      (identifier)
      (tuple_pattern (identifier) (identifier)))
    (identifier)))

============================================
Block
============================================

let x = {
  3
  4
}

---

(source_file
  (let_binding
    (identifier)
    (block
      (expression_statement (number))
      (expression_statement (number)))))
